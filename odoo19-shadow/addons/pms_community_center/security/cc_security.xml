<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Happiness Coin: Residents can only see their own coins -->
        <record id="pms_happiness_coin_resident_rule" model="ir.rule">
            <field name="name">PMS Happiness Coin: Resident Isolation</field>
            <field name="model_id" ref="model_pms_happiness_coin"/>
            <field name="groups" eval="[(4, ref('portal.group_portal')), (4, ref('base.group_user'))]"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
        </record>
    </data>
</odoo>
